const rHome = '/';
const rCategories = '/categories';
const rTable = '/tables';
const rForm = '/forms';

const RouterApp = {
    rHome,
    rCategories,
    rTable,
    rForm,
};

export default RouterApp;
