const rHome = '/';
const rFileManager = '/file-manager';
const rCategories = '/categories';
const rTable = '/tables';
const rForm = '/forms';

const RouterApp = {
    rHome,
    rFileManager,
    rCategories,
    rTable,
    rForm,
};

export default RouterApp;
