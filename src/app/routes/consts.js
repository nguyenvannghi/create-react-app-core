const rHome = '/';
const rLogin = '/login';
const rRegister = '/register';
const rCategories = '/categories';
const rTable = '/tables';
const rForm = '/forms';

const RouterApp = {
    rHome,
    rLogin,
    rRegister,
    rCategories,
    rTable,
    rForm,
};

export default RouterApp;
